package com.lab2.discussion.config;

import org.springframework.boot.test.context.TestConfiguration;

@TestConfiguration
public class TestBeansConfig {
    // Removed passwordEncoder bean to avoid conflict with SecurityConfig
} 