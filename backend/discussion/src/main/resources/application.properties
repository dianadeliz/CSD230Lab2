# App settings
server.port=8080

# MongoDB Atlas connection (or local if you're using localhost)
spring.data.mongodb.uri=mongodb://localhost:27017/discussionDB
spring.data.mongodb.database=discussionDB

# Disable Thymeleaf (not used since you’re building a frontend in React)
spring.main.web-application-type=servlet
spring.main.banner-mode=off

# Optional: Logging (for easier debugging)
logging.level.org.springframework.web=DEBUG
logging.level.org.mongodb.driver=INFO

# JWT (Optional if you want to use it in future)
jwt.secret=thisismyJWT
